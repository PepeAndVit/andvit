name: Message Slack

on:
  # Trigger the workflow on push or pull request,
  # but only for the main branch
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  slackNotification:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: Message Pull Request
        if: ${{ github.event.issue.pull_request }}
        run: |
          curl -H "Content-type: application/json" -X POST -d '{"channel": "#general", "title": "Pull Request", "text": "Esto es una pull request"}' ${{ secrets.SLACK_WEBHOOK }}
      - name: Message Push
        if: ${{ !github.event.issue.pull_request }}
        run: |
          curl -H "Content-type: application/json" -X POST -d '{"channel": "#inicio","attachments" :[{"title": "Amsterdam Apigee Infra","color" : "#F8FF3F","fields":[{"title" : "Description","value" : "Iria la descripción del action","short" : "true"},{"title" : "Priority/Ticket","value" : "Prueba del ticket","short" : "true"},{"title" : "Cause","value" : "https://github.com/PepeAndVit/andvit","short" : "true"},{"title" : "Solution","value" : "Prueba", "short" : "true"}]}]}' ${{ secrets.SLACK_WEBHOOK }}
